# 迁移与复现核验

核验日期：2026-09-14。分析数据日期为 2017 年；核验日期不代表新增业务数据。

## 本次实际执行

- **路线 A：** 用 DuckDB 1.5.5 从本地原始 `UserBehavior.csv` 重新读取 VARCHAR 五字段、按 `hash(user_id) % 100 = 0` 抽样，在独立验证数据库中运行完整下游 SQL 和 Python 导出。
- **路线 B：** 原有数据库被 IDE 内核占用，因此复制约 32 MiB 的数据库及其 WAL 作为验证快照；复制前后文件哈希一致。直接从快照的 `sample_events` 复跑下游，没有读取或复制原 CSV。未关闭用户的 IDE 内核。
- **抽样一致性：** 新抽样与原样本使用双向 `EXCEPT ALL` 比较，差异均为零；检查保留了完全相同行的数量。
- **结果一致性：** 两条路线导出的 10 份聚合 CSV 数值一致，原有 4 份汇总 CSV 也一致；浮点比较容差为 `1e-12`。
- **Notebook：** 迁移后的 25 个代码单元从头执行，无运行错误。执行时直接读取原 CSV 的少量记录检查字段，并复用路线 A 已生成的样本。关键表格、分解输出和内嵌趋势图已保存；原始事件与用户明细预览改为汇总展示。

## 已核对结果

| 核对项 | 查询结果 |
|---|---|
| 原样本 | 994,112 条；9,915 位用户 |
| 窗口筛选 | 前 526 条、窗口内 993,560 条、后 26 条 |
| 窗口内用户 | 9,915 |
| 完全相同行 | 五字段组合多出 1 条，保留 |
| 购买用户 | 1,407 → 1,747，净增 340 |
| 对称分解 | +416.75 −76.75 = +340（显示值四舍五入） |
| 用户分组 | +388 −9 −39 = +340 |
| 购买行为记录 | 2,096 → 2,605，净增 509 |
| 品类数 | 1,345：709 增长、491 下降、145 持平 |
| 品类变化合计 | +1,250 −741 = +509 |
| 增长前十 | +117，占正向增量 9.36% |

上述数值作为已知结果进行核对，没有写入 SQL 或 Python 作为分析输出。汇总计算同时核对每日总量、互斥用户组、品类购买记录以及分解残差。

## 范围与限制

使用当前本机已有的 Python 3.12.14 分析环境验证，核心依赖版本见 [requirements.txt](../requirements.txt)。没有另建空白环境重新下载所有依赖，没有重新从天池下载原数据，也没有验证其他 DuckDB 版本或其他数据文件。已有数据库的直接验证使用稳定快照；日常使用原库时仍需先释放其他进程的连接。

本次检查用于确认目录迁移和复现入口，不是新增业务研究或因果检验。原始 CSV、原库及 WAL 均保留在本地；只发布聚合结果。
